# Frozen Fork; 察看 is pull, not incremental inject

A Side Chat Fork is cut at the first send and then stays put. Follow-up questions about that moment are not interrupted when the 主会话 advances. When the Side Chat needs "now", it 察看s — the same shape as a 主会话 inspecting a subagent, not an automatic delta on every turn.

A one-turn incremental gate was rejected: it broke multi-question work about the frozen moment. Live inject was already rejected.
