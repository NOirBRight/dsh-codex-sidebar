# Find 主会话s by 列出, not by an inspect whitelist

Side Chat resolves "the one doing X" by listing 主会话s and then 察看ing, because the human often will not name an id. The roster is every unarchived 主会话 in this DSH Web profile, including other workspaces — cross-repo work is a requirement. It does not include subagents or other Side Chats: those are not 主Agent.

Restricting 察看 to the attached 主会话 plus an already-chosen 投递 target was rejected. Restricting the roster to the current cwd was rejected.
