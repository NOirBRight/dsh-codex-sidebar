# 察看 returns a 进度卡片

察看 is bounded: current turn/step, busy or idle, last visible conclusion, 本轮变更 file list. Details are read with the Side Chat's read-only file tools.

A full event slice since Fork was rejected: long 主会话 runs would recreate the "增量太多" problem. A busy/idle bit with no file list was rejected: the Side Chat would not know what to read.
