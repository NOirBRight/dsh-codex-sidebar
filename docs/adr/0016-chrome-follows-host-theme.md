# 侧栏 chrome follows the DSH host theme

Codex App is the IA reference (Palette, Tab strip, Browser empty, Side Chat empty, 侧栏开关). It is not a second color system. The 侧栏 uses the same theme tokens as the DSH host, including Terminal. A light Codex island on a dark 主会话 was tried in the prototype and rejected.

The page inside Browser may still be light: that is the loaded document, not chrome.
