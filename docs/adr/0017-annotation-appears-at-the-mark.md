# 批注 composer appears at the mark, after a click

Pencil only enters 批注. The composer is not a resident bar. It appears after the human clicks previewed content (a file line or a page element), next to that mark, and moves if they click another. Esc dismisses it. Enter still stacks on the 主会话 composer; Ctrl+Enter still sends (ADR 0003).

A dock that shows as soon as 批注 is on, or that stays pinned to the pane bottom, was rejected. Browser 批注 is only offered when a page is actually loaded — not on empty or unreachable chrome.
